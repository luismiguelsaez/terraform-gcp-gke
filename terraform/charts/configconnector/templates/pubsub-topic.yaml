{{- range $key, $value := .Values.gcloud.topics -}}
apiVersion: pubsub.cnrm.cloud.google.com/v1beta1
kind: PubSubTopic
metadata:
  annotations:
    cnrm.cloud.google.com/project-id: {{ $.Values.gcloud.project }}
  labels:
    environment: test
  name: {{ $value.name }}
{{- end -}}